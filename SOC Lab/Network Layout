# This file will show the Network Layout 

# The Network Layout will as the lab grows

# Starting 20 June 2026

# To support Windows + Linux + attacker systems, I will build Three virual switches in Hyper-V

1. Internal LAN
   - Purpose:
     - Domain Controller
     - Windows endpoints
     - Wazuh agents
     - Wazuh server
     - Ubuntu servers
     - SOC analyst workstations
   - This is my corporate network

2. DMS Network
   - Purpose:
     - Ubuntu web servers
     - Honeypots
     - Syslog collectors
     - Public-facing services
     - Anything I want attackers to hit.
   - Realistic attack surfaces.

3. Attacker Network
   - Purpose:
     - Kali Linux
     - C2 frameworks
     - Exploit testing
     - Malware staging
     - Pivoting into the LAN
   - Isolates offensive tools from your main network.

|        VM         |            Role             |    Network       |
|       ----        |            ----             |      ---         |
| **ServerDC1**     | Domain Controller           | Internal LAN     |
| **DHCP Server**   | DHCP (if separate)          | Internal LAN     |
| **DM1**           | Windows endpoint or server  | Internal LAN     |
| **DM2**           | Windows endpoint or server  | Internal LAN     |
| **ServerSA1**     | SOC Analyst workstation     | Internal LAN     |
| **ServerHyperV**  | Hyper‑V host                | Physical NIC     |
| **Kali Linux**    | Attacker                    | Attacker Network |
| **Ubuntu Server** | Web/Syslog/Honeypot         | DMZ              |
| **Wazuh Server**  | EDR + SIEM                  | Internal LAN     |